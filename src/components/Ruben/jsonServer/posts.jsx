import React from 'react'
import { link } from "react-router-dom"

export default function posts() {
  return (
    <div className='posts'>
    {posts.map(elem => {
        return (
            
        )
    })}
    </div>
  )
}
